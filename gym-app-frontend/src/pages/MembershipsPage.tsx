// src/pages/MembershipsPage.tsx
import { Typography } from 'antd';

const { Title } = Typography;

export function MembershipsPage() {
  return (
    <div>
      <Title level={2}>Абонементы</Title>
      <p>Здесь будет таблица абонементов и формы для управления ими.</p>
      <p>Это заглушка — реализуем в Модуле 5.</p>
    </div>
  );
}